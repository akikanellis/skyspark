apply from: 'scripts/common/dependencies.gradle'

buildscript {
    apply from: 'scripts/common/dependencies.gradle'

    repositories { jcenter() }
    dependencies {
        classpath gradlePlugins.shadowJar
    }
}